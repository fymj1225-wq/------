# 公開の手順

工場のPCを立ち上げなくても使えるように、置き場所をインターネット上へ移します。
上から順にやれば終わります。**詰まったら「うまくいかないとき」を見てください。**

---

## 用意するもの

| | |
| --- | --- |
| PC | Windows / Mac どちらでも |
| Node.js | [nodejs.org](https://nodejs.org/ja) の **LTS 版** |
| Fly.io のアカウント | 無料で作れます。支払い方法の登録を求められます |
| このフォルダ | 中身をそのまま使います |

> Fly.io 以外でも、Docker が動いて「消えないディスク」を付けられるところなら
> 同じ `Dockerfile` で置けます。料金は各社の設定次第なので、
> 最新の金額は必ず各社の料金ページで確認してください。

---

## 1. 合言葉を作る

このフォルダで、次を実行します。

```
node -e "console.log(require('crypto').randomBytes(18).toString('base64url'))"
```

`kZ8x...` のような24文字ほどの文字列が出ます。**これを控えておいてください。**
初回に一度だけ使い、あとは顔認証になります。

> 自分で考えた言葉でも構いませんが、**12文字未満は受け付けません**。

---

## 2. Fly.io の準備

```
npm install -g flyctl
fly auth signup        # 既にアカウントがあれば fly auth login
```

`fly version` が表示されれば入っています。

---

## 3. 名前を決める

`fly.toml` の1行目を開いて、`app = "restore-cost"` を好きな名前に変えます。
**世界で1つだけの名前**なので、`restore-cost-〇〇` のように工場名などを足してください。

```toml
app = "marutaka-restore"
```

この名前がそのままURLになります → `https://marutaka-restore.fly.dev`

---

## 4. 置き場所を作る

このフォルダで、上から順に実行します。

```
fly launch --no-deploy --copy-config --name あなたが決めた名前 --region nrt
fly volumes create restore_data --size 1 --region nrt
fly secrets set RESTORE_TOKEN='1で作った合言葉'
fly deploy
```

- `fly launch` … 途中でいくつか聞かれますが、**設定を変えるか聞かれたら No**（`fly.toml` をそのまま使います）
- `fly volumes create` … データを置く1GBのディスクです。写真込みでも当分足ります
- `fly secrets set` … 合言葉を預けます。`fly.toml` には書かないでください
- `fly deploy` … 3〜5分ほどかかります

最後に `https://あなたが決めた名前.fly.dev` と出れば完了です。

動いているか確認します。

```
fly status
fly logs
```

`レストア原価管理 — 共有サーバー` と出ていれば起動しています。

---

## 5. 携帯で開く

1. Safari で `https://あなたが決めた名前.fly.dev` を開く
2. 「データを開く」→ `合言葉で開く` → 1で作った合言葉を入れる
3. 「この端末の役割」→ **`親機にする`**
4. 「顔認証で開けるようにする」→ `登録する` → Face ID を通す
5. 共有ボタン →「ホーム画面に追加」

これで**アイコンから、顔認証だけで開けます**。合言葉はこの端末から消えています。

## 6. PCで開く

同じURLを開いて、

1. `合言葉で開く` → 合言葉を入れる
2. 「この端末の役割」→ **`子機にする`**
3. 顔認証（Windows Hello / Touch ID）も登録しておくと楽です

以降、PCは**開くたびに携帯の内容を取りに行きます**。

---

## 7. 見本データを消す

初回は見本の「ユーノス」が入っています。要らなければ
車両を開いて「車両情報」→ `車両を削除` で消してください。

---

## うまくいかないとき

| 症状 | 見るところ |
| --- | --- |
| `fly launch` で名前が使われていると言われる | 3に戻って別の名前にする |
| デプロイは通るが開けない | `fly logs` を見る。`RESTORE_TOKEN が必要です` と出ていたら4の `fly secrets set` をやり直す |
| 開くと真っ白 | `fly status` でマシンが動いているか確認。止まっていれば `fly deploy` をやり直す |
| 合言葉が通らない | 前後に空白が入っていないか確認。`fly secrets list` で登録日時を確認できる |
| 顔認証のボタンが出ない | `https://` で開いているか確認。IPアドレス（`192.168.…`）では使えません |
| 顔認証で失敗する | 端末側でFace IDが有効か確認。ダメなら `合言葉で開く` で入れます |
| 入れ替えたらデータが消えた | ディスクが付いていない可能性。`fly volumes list` に `restore_data` があるか確認 |

**それでも駄目なときは `fly logs` の中身をそのまま持ってきてください。** 一緒に潰します。

---

## アプリを直したとき

このフォルダの中身を変えたら、

```
fly deploy
```

端末側は開き直すと新しい版に切り替わります（「新しい版があります」と出ます）。

---

## そのほか

**合言葉を変える**
```
fly secrets set RESTORE_TOKEN='新しい合言葉'
```
顔認証を登録済みの端末はそのまま使えます。

**登録した端末を全部締め出す**（携帯を紛失したときなど）
```
fly ssh console -C "rm /data/credentials.json"
```
次からは全端末で合言葉が必要になります。合わせて合言葉も変えてください。

**バックアップ**
アプリの「バックアップ」ボタンでJSONを手元に落とせます。
節目ごとに取っておくのが確実です。サーバー側でも10分ごとに
`/data/backups/` へ自動で控えが残ります（最新60件）。

**止める**
```
fly apps destroy あなたが決めた名前
```
消す前に必ずバックアップを取ってください。
