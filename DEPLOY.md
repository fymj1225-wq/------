# 携帯で使えるようにする手順

**携帯1台だけで使う場合の手順です。** サーバーも料金もかかりません。

やることは「アプリのファイルを置く場所を1つ用意して、そのURLを携帯で開き、
ホーム画面に追加する」だけです。**入力したデータは携帯の中だけに残り、
置き場所には一切送られません。**

---

## なぜ「置く場所」が要るのか

iPhone は、手元のHTMLファイルをアプリとして起動できません。
一度どこかに置いて `https://…` のURLにしておく必要があります。

ただし置くのは**アプリの見た目と動きのファイルだけ**です。
車両のデータ・写真・金額は携帯の中から出ません。

---

## 手順（Netlify を使う場合・いちばん簡単）

### 1. フォルダを用意する

このフォルダをPCに置きます（GitHubから ZIP で落としてもOK）。

**見本の「ユーノス」のデータを外に出したくない場合**は、上げる前に
`assets/js/sample.js` の中身を全部消して、空のファイルにしてください。
（初回起動時に見本が入らなくなるだけで、アプリは普通に動きます）

### 2. Netlify にアカウントを作る

[netlify.com](https://www.netlify.com/) で無料のアカウントを作ります。
クレジットカードは要りません。

### 3. フォルダをドラッグして放り込む

ログイン後の画面に「Deploy manually / Drag and drop your site folder here」
という枠があるので、**このフォルダごとドラッグして落とします。**

数十秒で `https://ランダムな名前.netlify.app` が発行されます。

### 4. 名前を分かりやすくする（任意）

Site settings → Change site name で、好きな名前に変えられます。

```
https://marutaka-genka.netlify.app
```

### 5. 携帯で開く

1. Safari でそのURLを開く
2. 下の共有ボタン（□に↑）を押す
3. **「ホーム画面に追加」**

これで終わりです。ホーム画面にアイコンができます。

---

## Cloudflare Pages を使う場合

[dash.cloudflare.com](https://dash.cloudflare.com/) → Workers & Pages →
Create → Pages → **Upload assets** で、同じようにフォルダを上げられます。
無料、カード不要です。

## GitHub Pages を使う場合

GitHubのリポジトリ設定 → Pages → Branch に `claude/car-restoration-cost-app-0d87mo`
を選んで Save。数分で `https://ユーザー名.github.io/リポジトリ名/` が出ます。

> **注意**: GitHub Pages は基本的にリポジトリを公開する必要があります。
> `assets/js/sample.js` には見本の車両データ（仕入価格など）が入っているので、
> 公開する前に中身を空にしてください。

---

## 使いはじめたら

アイコンから起動すると、アドレスバーの無い全画面で立ち上がります。

- **電波が無くても起動して入力できます**（アプリ本体は携帯の中に入ります）
- 見本の「ユーノス」が要らなければ、開いて「車両情報」→ `車両を削除`
- 職人の名前は、上の `⋯` →「職人・単価の設定」で自分たちのものに変えてください

---

## いちばん大事なこと：バックアップ

**データは携帯の中だけにあります。** 便利な反面、

- 携帯を失くす・壊す
- Safariの「履歴とWebサイトデータを消去」を押す

これで消えます。**節目ごとにバックアップを取ってください。**

### 取り方

上の `⋯` →「バックアップを保存」、または左上のランプ →「バックアップを取る」。

携帯では**共有シート**が開くので、そのまま

- 自分宛にLINEで送る
- メールで送る
- 「ファイルに保存」で iCloud Drive に入れる

のどれかをしておけば安心です。**1台仕上がったタイミングが目安です。**

しばらく取っていないと、起動時にそっと知らせます（タップでそのまま取れます）。

### 戻し方

左上のランプ →「バックアップから戻す」で、保存したJSONを選ぶだけです。

---

## アプリを直したとき

新しいフォルダを同じ手順でもう一度上げ直せば入れ替わります。
携帯側は開き直すと「新しい版があります」と出て切り替わります。

**入れ替えても、携帯の中のデータは消えません。**

---

## あとからPCとも共有したくなったら

同じアプリのまま、置き場所にサーバーを立てれば共有できます。
親機（携帯）・子機（PC）の仕組みと顔認証もそのまま入っています。
必要になったら README の「PCと携帯で同じデータを見る」を見てください。
